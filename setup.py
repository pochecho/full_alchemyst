from distutils.core import setup
setup(
  name = 'full_alchemyst',
  packages = ['full_alchemyst'], # this must be the same as the name above
  version = '0.0.1',
  description = "Les's to use a unique ORM to manage the persistence in one project. ",
  author = 'Sergio Posada Urrea',
  author_email = 'alejandro@geekytheory.com',
  url = 'https://github.com/{user_name}/{repo}', # use the URL to the github repo
  download_url = 'https://github.com/{user_name}/{repo}/tarball/0.1',
  keywords = ['testing', 'logging', 'example'],
  classifiers = [],
)